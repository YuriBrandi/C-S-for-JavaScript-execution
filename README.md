# C-S-for-JavaScript-execution
C/S made in Java for executing JavaScript code sent by Client to a multi-client Server. Includes GUI and XOR encryption. Communication implemented through JSON strings.
